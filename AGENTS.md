# Project Notes

This is a user-owned local React application. Keep changes focused, preserve
existing visual behavior, and validate changes with the scripts in
`package.json`.

The application uses Vite, React, browser local storage, and local assets.
Do not add hosted application backends or secrets to the repository.
